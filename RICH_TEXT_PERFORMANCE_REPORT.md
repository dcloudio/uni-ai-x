# Rich Text 流式渲染性能排查报告

日期：2026-07-28

分支：`dev-vapor-richTextNative`

测试端：Android，ADB 设备 `192.168.2.5:5555`

工具链：HBuilderX `5.23.2026072701-dev`，uni-app x 蒸汽模式，视图层字节码，自定义基座

## 1. 最终结论

当前流式 Markdown 的主要瓶颈不是 CMark 解析，而是 Android 原生视图层在 Rich Text 节点变化后执行的重新布局、位图快照和 View 追加。

最小复现中，固定不变的超宽原生 Rich Text 在追加兄弟节点时保持约 `117 FPS`，没有重复位图拷贝；持续修改同一个 Rich Text 的 `nodes` 后下降到 `69.1 FPS`，并在 4.57 秒内观测到 91 次 `RGBA -> Bitmap` 拷贝，累计搬运约 `625 MB`。这说明卡顿触发条件是“原生 Rich Text 内容持续变化”，不是“页面中存在 Rich Text”本身。

应用侧优化已经减少了重复发布、流式阶段的超宽临时位图和图片插入时的解码等待。真实页面多轮测试中，原生布局累计耗时相对优化前下降约 `9%~24%`，渲染任务累计耗时下降约 `2%~13%`；但整体帧率仍在 `97~99 FPS` 左右波动，30~90ms 的主线程尖峰没有消失。剩余问题需要框架层优化原生 Rich Text 的增量更新和位图快照链路。

## 2. 测试方法

本次同时使用了三组证据，避免只看 Android“显示布局边界”或只看 FPS 数字造成误判：

1. 页面侧 `requestAnimationFrame`：统计平均 FPS、最大帧间隔和超过 16/32ms 的帧。
2. uni-app x 原生渲染日志：统计 `executeRenderTasks`、`renderNativeLayoutTasks`、`appendViewTasks`。
3. Rich Text 原生日志：统计 `Tile RGBA->Bitmap copy` 和 `BuildTileSnapshotList` 的次数、尺寸与耗时。

最小复现页按顺序自动运行不同场景，测试完成后已从 `pages.json` 和源码目录清除，没有留在最终产品中。

注意：所有数据来自调试基座。调试日志、布局边界和 GPU 柱状图本身有额外开销；设备刷新率也会动态变化，因此应以数量级、重复结果和原生分项耗时判断，不应把一次 FPS 小数点变化当成确定收益。

## 3. 基线与优化结果

### 3.1 最早基线

| 指标 | 最早结果 |
| --- | ---: |
| 页面平均 FPS | 约 95.4 |
| 最大帧间隔 | 91.3ms |
| CMark 调用 | 60 次 |
| CMark 累计耗时 | 约 1753ms |
| CMark 最大单次耗时 | 121ms |
| CMark 线程 | 主线程 |

这时解析和渲染都可能阻塞主线程，无法判断 Rich Text 自身占比。

### 3.2 解析移入子线程后

| 指标 | 当前结果 |
| --- | ---: |
| CMark 调用 | 62 次 |
| CMark 累计耗时 | 41ms |
| CMark 平均耗时 | 0.66ms |
| CMark 最大单次耗时 | 7ms |
| 应用主线程 TID | 8246 |
| CMark TID | 11170（单一后台线程） |

CMark 已经不在主线程执行。解析仍消耗 CPU、电量和内存带宽，也仍应避免无效调用，但它已不是当前可见卡顿的直接来源。

### 3.3 真实 Markdown 流式页面

测试窗口从第一次 CMark 调用开始，到最后一次调用后 2 秒，排除了 398ms 和 773ms 的首次页面冷启动任务。

| 指标 | 优化前代表值 | 优化后重复测试 | 本轮最终回归 |
| --- | ---: | ---: | ---: |
| 渲染任务次数 | 307 | 192 / 297 | 276 |
| 渲染任务累计耗时 | 1388ms | 1233 / 1366ms | 1208ms |
| 原生布局累计耗时 | 818.84ms | 660.91 / 746.25ms | 619.17ms |
| 原生布局最大单次 | 约 58ms | 约 73ms | 69.11ms |
| View 追加累计耗时 | 426.44ms | 508.79 / 470.63ms | 498.64ms |
| View 追加最大单次 | 约 44ms | 约 40ms | 40.65ms |
| 平均 FPS | 98.8 | 96.9 / 99.3 | 未注入页面探针 |
| 最大帧间隔 | 74.68ms | 约 91ms | 渲染任务最大 90ms |

结论：原生布局累计耗时有稳定改善，但整体 FPS 没有同等幅度提升。原因是剩余尖峰仍超过单帧预算，且 View 追加耗时没有下降。

优化前，流式代码曾生成 `2380x595`、`2989x595`、`3899x595` 的中间 Rich Text 位图。限制临时内容宽度后，中间两个超宽尺寸消失，仅在内容结束后生成最终可横向滚动的宽内容。本轮最终回归未观察到 RichTextJNI 的重复大位图拷贝告警。

## 4. 最小复现实验

### 4.1 页面帧率

| 场景 | FPS | 最大帧间隔 | >16ms | >32ms |
| --- | ---: | ---: | ---: | ---: |
| 普通 `scroll-view` 追加 View | 114.9 | 16.62ms | 23 | 0 |
| 固定 Rich Text + 追加兄弟 View | 116.8 | 16.61ms | 15 | 0 |
| 持续修改 Rich Text nodes | 69.1 | 66.35ms | 84 | 47 |
| 单个大 `list-item` 内追加 | 105.1 | 33.19ms | 44 | 5 |
| 拆成多个 `list-item` 追加 | 98.7 | 24.92ms | 82 | 0 |
| 图片预挂载后只改 opacity | 118.7 | 16.56ms | 1 | 0 |
| 动态插入同一图片 | 118.1 | 24.90ms | 1 | 0 |

### 4.2 原生任务与位图快照

| 场景 | 原生渲染任务累计 | 原生布局累计 | View 追加累计 | Rich Text 位图证据 |
| --- | ---: | ---: | ---: | --- |
| 普通追加 | 83ms | 59.35ms | 8.31ms | 无 |
| 固定 Rich Text + 兄弟追加 | 79ms | 56.61ms | 8.24ms | 无 |
| 修改 Rich Text nodes | 日志队列仅记录 13ms | 10.08ms | 0 | 91 次拷贝、约 625MB；快照日志累计 444.54ms |
| 单个大 `list-item` | 212ms | 162.08ms | 9.26ms | 无重复拷贝 |
| 多个 `list-item` | 231ms | 153.87ms | 47.51ms | 无重复拷贝 |
| 图片 opacity 切换 | 1ms | 0.35ms | 0 | 无 |
| 动态插入图片 | 21ms | 2.37ms | 13.87ms | 无 |

Rich Text 位图快照耗时与拷贝耗时存在包含关系，不能把两者直接相加。重要信号是：4.57 秒内至少有约 625MB 像素数据被重复搬运，而稳定 Rich Text 场景没有发生该行为。

## 5. 已证伪的假设

### 5.1 “页面只要存在超宽 Rich Text，追加任何节点都会重绘它”

不成立。固定 `3822px` 宽 Rich Text 时，追加兄弟 View 仍维持约 117 FPS，测试阶段没有重复快照。只有 Rich Text 的 `nodes` 本身变化才触发高频拷贝。

### 5.2 “流程图图片插入就是全部卡顿来源”

不成立。动态插入 800x640 SVG 图片会产生约 21ms 渲染任务，其中 View 追加约 13.87ms；但它解释不了持续数秒的 30~90ms 尖峰。预挂载并在加载后切换 opacity 基本没有额外任务，因此该应用侧规避仍然有效。

### 5.3 “把一个长消息拆成多个 list-item 一定更快”

不成立。本轮最小复现中，拆分后平均 FPS 从 105.1 降到 98.7，View 追加累计从 9.26ms 增至 47.51ms。不能仅根据节点层级或 flatten 边界进行这种大规模结构改造。

### 5.4 “CMark 已在子线程，因此其耗时完全不需要关注”

不完全成立。它已经不直接占用 UI 线程，但仍占用 CPU、内存和电量，完成回调也会触发主线程数据发布。当前单次解析很轻，真正应控制的是发布后引起的视图更新次数，而不是继续微优化这 0.66ms 的解析本身。

## 6. 应用侧已落地优化

### 6.1 Android 解析线程复用

`asyncMd2json` 使用单线程 Executor，并复用异步 CMark 实例，不再为每次增量解析创建 `Thread` 和 `MainActivity`。回调统一投递回主线程。

### 6.2 合并和跳过无效发布

- 解析请求继续按 300ms 节流。
- 新增 48ms token 发布合并窗口。
- 相同文本、相同完成状态不重复解析。
- token 引用列表未变化时不重复通知视图层。
- 已关闭且内容不变的 Markdown token 继续复用对象，避免已完成块被当作新节点。

### 6.3 限制流式临时超宽内容

- 未结束的代码块只按可见宽度渲染，并按折行后的视觉行数计算高度。
- 未生成图片的数学公式源码同样限制在可见宽度。
- 内容结束后恢复最终宽度，保留横向滚动和完整内容展示。

该改动消除了流式阶段逐步增长的超宽中间位图，是本轮最明确的应用侧收益来源。

### 6.4 公式和流程图图片预挂载

- 图片先以 `opacity: 0` 挂载并等待 `load`。
- 解码完成后再显示图片，并移除临时 Rich Text 源码。
- 流程图“流程图/代码”切换仍保留，图片预览仍使用原有 `previewImage`。

最小复现显示，预挂载切换约 1ms，动态插入约 21ms。

### 6.5 v2-beta 分支复核

`v2-beta` 中有效的 Android 子线程解析和鸿蒙解析补全已经通过当前分支的 `78ec6d7`、`2a0a64e` 等提交进入当前实现。本轮又补充了线程池和解析器实例复用。

没有搬运 `v2-beta` 中回退到 `scroll-view`、把解析间隔改为 120ms 等旧实现：当前分支的 `list-view`、300ms 输入/解析节流与 token 复用更符合本轮性能数据，直接覆盖会增加更新频率并倒退现有交互修复。

## 7. 剩余应用侧限制

1. 流式 Markdown 的最后一个开放块必然会变化。应用只能降低更新频率、限制临时尺寸，不能在保证实时显示的同时完全消除节点更新。
2. 最终超长代码仍必须生成完整宽度内容以支持横向滚动，但它现在只在关闭代码块后发生一次。
3. 公式和流程图从临时源码切换为图片时仍会有一次尺寸变化和布局；继续隐藏该变化会牺牲内容及时展示。
4. 进一步降低发布频率可以减少卡顿次数，但会明显降低流式输出的连续感。目前 300ms 解析节流加 48ms 发布合并是偏保守的平衡点。

## 8. 建议框架侧处理的问题

### P0：原生 Rich Text nodes 更新触发全量位图快照

复现条件：`rich-text mode="native"`，内容宽度约 3822px，每 100ms 更新一次 `nodes`。

现象：FPS 从约 117 降至 69；4.57 秒内观测 91 次 RGBA 到 Bitmap 拷贝，约 625MB；单次快照最高 11.17ms。

建议：

- 对 nodes 做结构/文本差量更新，复用未变化段落和 tile。
- 内容只在尾部增长时，仅重新排版和栅格化受影响区域。
- 横向超出视口的内容按可见区或惰性 tile 生成，避免每次复制完整宽度位图。
- 避免每次快照都进行 RGBA 到 Java Bitmap 的全量 CPU 拷贝，优先复用纹理或共享缓冲区。

### P0：原生布局和 View 追加存在长主线程任务

真实流式阶段仍观测到：

- `renderNativeLayoutTasks` 最大 69.11ms。
- `appendViewTasks` 最大 40.65ms。
- 单次 `executeRenderTasks` 最大 90ms。

建议在框架 Trace 中进一步拆分 Rich Text measure/layout、节点绑定、原生 View 创建、图片属性提交等阶段，并考虑把可拆分工作分帧执行。当前日志只暴露队列总耗时，应用侧无法再定位到具体组件内部函数。

### P1：大图首次绘制与滚动复用

最终页面的 `dumpsys gfxinfo` 显示 GPU 90/95 分位约 5/8ms，说明常态 GPU 不是主瓶颈；但仍记录 41 次 slow bitmap upload，滚动首次遇到流程图时可能发生纹理上传或图片 View 重绑定。

建议检查：

- 已加载图片进入可见区时是否重复上传纹理。
- `list-view` 内大图离屏/回屏时是否保留解码结果和 GPU 资源。
- SVG data URL 到 Bitmap 的缓存键是否稳定，暗黑主题变体是否可复用。
- 是否能提供“预解码并预上传”能力，而不要求应用依靠透明挂载实现。

## 9. 最终验证

- HBuilderX Android 全量编译成功：工程 3 个正式页面，无临时性能页。
- 自定义基座同步并启动成功。
- 流式示例完整渲染到数学公式和流程图，设备截图确认内容及布局正常。
- CMark 62 次调用全部位于后台 TID 11170。
- 日志未发现 `FATAL EXCEPTION`、UTS 运行时错误或 Markdown 解析错误。
- `git diff --check` 通过。
- 流程图/代码切换此前已由人工设备验证；本轮 ADB 输入注入被设备系统策略禁止，因此没有伪造自动点击结论。

## 10. 最终文件范围

本轮产品改动仅涉及：

- `uni_modules/uni-ai-x/components/uni-ai-msg-code/uni-ai-msg-code.uvue`
- `uni_modules/uni-ai-x/components/uni-ai-msg-math/uni-ai-msg-math.uvue`
- `uni_modules/uni-ai-x/sdk/parseMarkdown.uts`
- `uni_modules/uni-cmark/utssdk/app-android/index.uts`

`manifest.json` 的版本号改动是工作区原有改动，本轮未修改或回退。临时性能页面与 `pages.json` 路由已经清除。
