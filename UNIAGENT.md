# UNIAGENT Rules

这个文件用于记录本项目中 `uni-agent` 需要长期遵守的协作规则。后续对话里形成的新稳定规则，应继续迭代到这里。

## 调试产物

- 所有调试截图、临时验证图片、运行期采样图片，统一放到 `uni-agent-artifacts/`。
- 不要把 `.png`、`.jpg`、`.jpeg`、`.webp` 这类临时产物直接放在项目根目录。
- `uni-agent-artifacts/` 默认不提交到 Git；只保留目录本身作为约定位置。

## 验证流程

- 如果对问题只有假设，优先在单独的空白页或专用 debug 页验证。
- 只有在单页或隔离验证通过后，才回到真实业务环境让用户介入复核。
- 如果真实业务环境出现问题，先根据用户截图和现象描述提出最小假设，再回到单页做最小复现。
- 不要把复杂集成环境当作第一验证场，避免缓存、会话状态、流式时机等变量干扰判断。
- UTS 调试环境会把字符串里的 `console.log(...)` 展示成编译后的 `uni.__log__(...)` 形式，这是已知调试期表现，生产环境不会遇到；不要把它当作 `rich-text`、Markdown 或代码块渲染问题反复排查。

## 首页模式

- `uni_modules/uni-ai-x/sdk/testMarkdownText.uts` 留空时，首页按正式业务模式运行。
- 需要隔离验证内置 Markdown 示例时，优先使用单独验证页和独立示例数据源，不要直接污染首页真实业务入口。

## 协作约定

- 当用户指出新的长期习惯或仓库级约束时，把它整理并追加到本文件。
- 做文件整理或产物归档时，优先保持最小改动，不影响现有业务代码路径。
- 新增或修改仓库内的 Markdown 说明文档时，默认使用中文撰写；没有明确要求时，不要提交英文 Markdown 文档。
