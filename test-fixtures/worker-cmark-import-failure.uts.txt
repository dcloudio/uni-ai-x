// Noncompiled fixture: copy these imports into a WorkerTaskImpl source to reproduce.
// Tested with HBuilderX 5.24.2026072917-dev on 2026-07-30.

import { md2json } from '@/uni_modules/uni-cmark'
import { ParseMdRes } from '@/uni_modules/uni-cmark/utssdk/interface.uts'

export class CMarkImportProbe extends WorkerTaskImpl {
	override entry() {
		const result = md2json('# worker cmark probe') as ParseMdRes
		this.postMessage({ phase: 'cmark-success', result: result } as UTSJSONObject)
	}
}

/*
Observed Android compile error:

[plugin:uts] load_transformed failed
failed to resolve ../uni_modules/uni-cmark from
unpackage/dist/dev/.uvue/app-android/workers/markdownWorkerProbe.uts
index not found

This fixture has a .txt suffix so the normal app build remains runnable.
*/
