<template>
	<view class="input-tool-bar uni-theme-root">
		<view class="input-tool-bar-btn" :class="[thinkingActive ? 'input-tool-bar-btn-active' : '', !canThinking ? 'input-tool-bar-btn-disable' : '']" @click="setThinking"
					<!-- #ifdef WEB -->
					:title="thinkingTitle"
					<!-- #endif -->
				>
            <uni-ai-icon code="e639" :color="!canThinking ? '#aaa' : (thinkingActive ? '#3c76e4' : '#666')"></uni-ai-icon>
            <text class="input-tool-bar-text" :class="[thinkingActive ? 'input-tool-bar-text-active' : '', !canThinking ? 'input-tool-bar-text-disable' : '']">深度思考</text>
		</view>
			<view class="input-tool-bar-btn input-tool-bar-search" :class="[webSearch ? 'input-tool-bar-btn-active' : '']" @click="setWebSearch">
            <uni-ai-icon code="e797" :color="webSearch ? '#3c76e4' : '#666'"></uni-ai-icon>
            <text class="input-tool-bar-text" :class="[webSearch ? 'input-tool-bar-text-active' : '']">联网搜索</text>
			</view>
			<uni-icons v-if="canParseImg" @click="uploadImage" class="input-tool-bar-upload-image-btn" type="plusempty" size="25" color="var(--text-secondary)"></uni-icons>
		<view v-if="aiState == 'processing'" class="input-tool-bar-stop input-tool-bar-stop-active"
			@click="stop">
            <uni-ai-icon code="e691" color="#FFF" size="14"></uni-ai-icon>
        </view>
		<view v-else class="input-tool-bar-send" @click="send" :class="inputContent.length > 0 && allUploaded ? 'input-tool-bar-send-active' : ''"
					<!-- #ifdef WEB -->
					:title="allUploaded ? '发送' : '请等待图片上传完成'"
					<!-- #endif -->
				>
            <uni-ai-icon code="e627" color="#FFF" size="20"></uni-ai-icon>
        </view>
    </view>
</template>
<script lang="uts" setup>
import uniAi, { isCustomProvider } from '@/uni_modules/uni-ai-x/sdk';
import {llmModelMap, defaultLLM, type LLMModel} from '../config';
import { uploadDraftImages } from '@/uni_modules/uni-ai-x/sdk/image-upload.uts';

const isOpenClaw = computed<boolean>(() => {
    return isCustomProvider(uniAi.llm.provider);
});


const aiState = computed<string>(() => {
    return uniAi.currentChat?.state ?? 'none'
});

const inputContent = computed<string>({
    get: (): string => {
        return uniAi.currentChat?.inputContent ?? ''
    },
    set: (value: string) => {
        if (uniAi.currentChat != null) {
            uniAi.currentChat!.inputContent = value
        }
    }
})

const canParseImg = computed<boolean>(() => {
    return true
});

const currentLLMModel = computed<LLMModel>(() => {
    const provider = uniAi.llm.provider
    const model = uniAi.llm.model
    const currentProviderInfo = llmModelMap.get(provider)
    if (currentProviderInfo == null) {
        if (!isCustomProvider(provider)) {
            uni.showModal({
                content: 'provider：' + provider + ' 不可用，请到系统设置中切换为其他 provider',
                showCancel: false
            });
            console.error('provider：' + provider + ' 不可用，请到系统设置中切换为其他 provider')
        }
        return { modelId: model.length > 0 ? model : defaultLLM.model } as LLMModel
    }
    return currentProviderInfo.models.find(m => m.modelId == model) ?? currentProviderInfo.models[0]!
});

const webSearch = computed<boolean>(() => {
    return uniAi.llm.webSearch as boolean;
});

const stop = async () => {
    uniAi.currentChat!.state = 'stop';
    uniAi.abortRequest()
    await nextTick()
};

const allUploaded = computed<boolean>(() => {
    return uniAi.imgList.every(img => img.uploadPercent == 100);
});

const send = async () => {
    if (inputContent.value.length == 0) {
        return;
    }
    if (!allUploaded.value) {
        uni.showToast({
            title: '请等待图片上传完成',
            duration: 3000,
            icon: 'none'
        });
        return;
    }
    uniAi.sendMsg(inputContent.value)
    inputContent.value = '';
};

const canThinking = computed<boolean>(() => {
    return currentLLMModel.value.thinking ?? false;
});

// 深度思考当前是否开启：模型支持思考，且用户未显式关闭（未设置时默认开启）
const thinkingActive = computed<boolean>(() => {
    return canThinking.value && (uniAi.llm.thinking ?? true);
});

// Web 端 hover 提示：不支持时提示不可操作，支持时提示切换动作
const thinkingTitle = computed<string>(() => {
    if (!canThinking.value) {
        return '当前模型不支持操作'
    }
    return thinkingActive.value ? '关闭深度思考' : '开启深度思考'
});

const setThinking = () => {
    if (isOpenClaw.value) {
        uni.showToast({
            title: uniAi.llm.provider + '不支持深度思考',
            icon: 'none'
        });
        return
    }
    if (!canThinking.value) {
        uni.showToast({
            title: '当前模型不支持深度思考',
            duration: 3000,
            icon: 'none'
        });
        return
    }
    const enabled = !thinkingActive.value
    uniAi.llm.thinking = enabled
};

const setWebSearch = () => {
    uniAi.llm.webSearch = !webSearch.value;
};

function uploadImage() {
    uploadDraftImages(currentLLMModel.value)
}

onMounted(() => {
    // #ifdef WEB
    document.addEventListener('keydown', async (e) => {
        if (e.key == 'Enter' && !e.ctrlKey && !e.shiftKey && !e.altKey && !e.metaKey && !e.isComposing) {
            e.preventDefault()
            if (aiState.value == 'processing') {
                await stop()
            }
            send()
        }
    })
    // #endif
});

</script>
<style lang="scss">
@import '@/uni_modules/uni-ai-x/static/css/variables.scss';

.input-tool-bar {
    flex-direction: row;
    align-items: center;
    padding: 5px 0;
}

.input-tool-bar-btn {
    flex-direction: row;
    align-items: center;
    padding: 5px 10px;
    border-radius: 30px;
    margin-right: 5px;
    background-color: var(--input-btn-bg);
}

.input-tool-bar-btn-active {
    background-color: var(--input-btn-active-bg);
}

.input-tool-bar-btn-disable {
    /* #ifdef WEB */
    cursor: not-allowed;
    /* #endif */
}

.input-tool-bar-text {
    font-size: 12px;
    color: var(--text-secondary);
    margin-left: 3px;
}

.input-tool-bar-text-active {
    color: var(--accent-primary);
}

.input-tool-bar-text-disable {
    color: var(--text-disabled);
}

.input-tool-bar-search {
    margin-right: auto;
}

.input-tool-bar-stop,
.input-tool-bar-send {
    background-color: var(--accent-secondary);
    width: 30px;
    height: 30px;
    justify-content: center;
    align-items: center;
    border-radius: 100px;
}

.input-tool-bar-stop-active,
.input-tool-bar-send-active {
    background-color: var(--accent-primary);
}

.input-tool-bar-upload-image-btn {
    margin-right: 15px;
}

</style>
